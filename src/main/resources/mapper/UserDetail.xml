<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.enjoytrip.dao.user.UserDetailDao">

	<select id="countAttractionBookmark" parameterType="java.lang.Long">
		SELECT COUNT(*)
		FROM hotplace_bookmark_favor
		WHERE user_id = #{userId}
		AND bookmark = 1
	</select>
	
	<select id="countAttractionFavor" parameterType="java.lang.Long">
		SELECT COUNT(*)
		FROM hotplace_bookmark_favor
		WHERE user_id = #{userId}
		AND favor = 1
	</select>
	
	<select id="countHotplace" parameterType="java.lang.Long">
		SELECT COUNT(*)
		FROM hotplace
		WHERE user_id = #{userId}
	</select>
	
	<select id="countPlanBookmark" parameterType="java.lang.Long">
		SELECT COUNT(*)
		FROM plan_user_bookmark_favor
		WHERE user_id = #{userId}
		AND bookmark = 1
	</select>
	
	<select id="countPlanFavor" parameterType="java.lang.Long">
		SELECT COUNT(*)
		FROM plan_user_bookmark_favor
		WHERE user_id = #{userId}
		AND favor = 1
	</select>
	
	<select id="countPlanParticipate" parameterType="java.lang.Long">
		SELECT COUNT(*)
		FROM plan_user
		WHERE user_id = #{userId}
	</select>

</mapper>